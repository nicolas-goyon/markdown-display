const footer = () => {
    // TODO : ajouter le footer et fixer le bug qui fait que le footer n'est pas fixé en bas de la page
    return (
        `
        
        </body>
        </html>
        `
    )
}

module.exports = footer;